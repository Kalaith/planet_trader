# Outstanding tasks

- [ ] Add visible previous/next controls to `src/ui/workshop.rs::draw_portfolio`; it displays only three owned worlds and `ScrollInventory` currently comes only from the mouse wheel. Verify selecting the fourth and later worlds using touch alone (CODE_STANDARDS §7.5).
- [ ] Add a visible LOAD control on the Company screen wired to `UiAction::Load`. Update `README.md` and `game_page.json` to name the actual touch equivalents for loading and scrolling; change the tutorial's vague scan instruction to name SCAN FOR PLANET CONTRACTS (§7.5).
- [ ] Expose intentional testable APIs through `src/lib.rs`, make `main.rs` use the library, and migrate all seven `src/**/tests.rs` suites and test-only helpers into `tests/`. Remove source-level test wiring and the unused production wrapper `GameSession::apply_tool` with its `allow(dead_code)`; update tests to exercise `apply_tool_with_intensity`. Remove the obsolete test-only `market_refresh_label` and its test. Organize retained coverage by feature, targeting five cases per feature without losing useful regressions (§1.4, §11).
- [ ] Add semantic validation to `src/data.rs::GameData::load` after toolkit parsing: reject duplicate/empty identifiers, unresolved tool/research/knowledge references, prerequisite cycles, invalid costs and environmental ranges, unknown effect keys, and unusable content lists. Return source/entity-labeled errors and add focused invalid-data coverage after the test migration (§5.3, §6).
- [ ] Move hardcoded economy and progression configuration from `src/state.rs` and `src/state/{progression,terraforming}.rs` into typed JSON under `assets/`: generation ranges, sale/salvage/research rewards, rank thresholds, contract counts, intensity multipliers, and forecast uncertainty. Replace research-name-based capability checks with validated data references (§5.3).
- [ ] Move player-facing labels, tutorial prompts, notifications, error templates, rank/knowledge names, and discovery hints from `src/ui/**`, `src/game.rs`, and `src/state/**` into JSON loaded through the toolkit. Keep visible control labels and tutorial references consistent (§5.3, §7.5).
- [ ] Extract cohesive helpers from functions exceeding 100 lines, including `Game::begin_capture_scene`, `Game::apply_action`, purchase-modal rendering, workshop tool/inspector rendering, market buyer/deal rendering, and research lab/node-card rendering. Move capture setup and action handling into named modules where appropriate; retain the empty-exception 800-line source gate (§2.2, §4.1).
- [ ] Centralize rank-progress and sale-eligibility calculations currently duplicated in `src/ui/company.rs` and `src/ui/market_deck.rs` with their state rules. Return named results for rank information and forecast ranges instead of tuples, and keep renderers reading results and emitting intents (§4.4, §5.1, §7.1).
- [ ] Add missing module-purpose `//!` comments to child modules under `src/ui/` and `src/state/`, and replace parameter shadowing such as `alien_index`'s `let name` in `src/artwork.rs` with descriptive distinct names (§9.2, §10.3).
